{# import all variables from a central location -#}
{% import "consul/variables.jinja" as vars -%}

server {
  listen       80;
  server_name  _;

  # pass consul ui
  location /consul/ {
    proxy_pass http://consul:{{vars.consul_port_http}}/ui;
    proxy_redirect /ui /consul;
  }
}